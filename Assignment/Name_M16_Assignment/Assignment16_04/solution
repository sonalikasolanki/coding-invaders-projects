function fun(N, M) {
    var arr = [];
    var counter = 0;
  
    for (var i = 0; i < N; i++) {
      arr.push(i);
    }
  
    for (var j = 0; j < M; j++) {
      counter += 1;
    }
  
    console.log(counter);
  }

  //time comlexity is O(M)
  